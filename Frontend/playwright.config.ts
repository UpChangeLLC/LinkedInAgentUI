import { defineConfig, devices } from '@playwright/test'

/**
 * E2E config for the onboarding flow (Workstream E / gate G2).
 *
 * Self-contained: builds the frontend and serves it with `vite preview`, then
 * the specs mock every backend call (`/mcp/*`, `/api/*`) via Playwright route
 * interception. No backend, database, Apify, or LLM keys are required — runs
 * deterministically in CI.
 */
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: process.env.CI ? [['github'], ['html', { open: 'never' }]] : 'list',
  timeout: 60_000,
  expect: { timeout: 15_000 },
  use: {
    baseURL: 'http://localhost:4173',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
  webServer: {
    command: 'npm run build && npm run preview -- --port 4173 --strictPort',
    url: 'http://localhost:4173',
    reuseExistingServer: !process.env.CI,
    timeout: 180_000,
  },
})
